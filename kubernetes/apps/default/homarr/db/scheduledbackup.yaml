---
apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: homarr-db-backup
spec:
  schedule: "0 30 2 * * 0"
  immediate: true
  backupOwnerReference: self
  cluster:
    name: homarr-db
  method: plugin
  pluginConfiguration:
    name: barman-cloud.cloudnative-pg.io
