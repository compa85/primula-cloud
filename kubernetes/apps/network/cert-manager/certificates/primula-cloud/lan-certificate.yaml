apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: primula-cloud-lan
spec:
  secretName: primula-cloud-lan-tls
  issuerRef:
    name: primula-cloud-production-issuer
    kind: ClusterIssuer
  dnsNames:
    - "*.lan.${SECRET_DOMAIN}"
